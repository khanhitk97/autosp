name: Build iOS Dylib

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14

    steps:
      - name: Checkout mã nguồn
        uses: actions/checkout@v4

      - name: Cài đặt ldid & Theos Toolchain
        run: |
          # Cài đặt ldid để ký dylib
          brew install ldid
          
          # Cài đặt Theos
          git clone --recursive https://github.com/theos/theos.git $HOME/theos
          echo "THEOS=$HOME/theos" >> $GITHUB_ENV
          
          # Tải iOS SDK
          curl -LO https://github.com/theos/sdks/archive/master.zip
          unzip -q master.zip
          mv sdks-master/*.sdk $HOME/theos/sdks/
          rm -rf master.zip sdks-master

      - name: Biên dịch Dylib (Compile)
        run: |
          export THEOS=$HOME/theos
          make package FINALPACKAGE=1
          
          # Gom file .dylib và .deb vào thư mục output
          mkdir -p output
          find .theos/obj -name "*.dylib" -exec cp {} output/ \;

      - name: Lưu trữ và Tải xuống file Dylib (Artifacts)
        uses: actions/upload-artifact@v4
        with:
          name: DriverAssistantPro-Dylib
          path: |
            output/*.dylib
            packages/*.deb
